static int[] gradingStudents(int[] grades) {
       
         for(int i=0;i<grades.length;i++){
             int n = grades[i]%5;
             if(grades[i]>=38 && n>=3){
                 grades[i]+=5-n;
             }
         }
         return grades;

    }
